- .local 文件夹中的文件均为本地 demo、example，不参与 Git 追踪也不参与博客内容，通常用于参考
- 每当你完成一个任务，给出符合约定时提交的 commit message；若用户要求则可以提交，否则默认只给出消息不提交
- 面向访客的内容只放在 `docs/**/*.md`，这些页面是公开博客内容，不得混入维护者说明
- 面向编辑者/维护者的文档统一放在 `editor-docs/`，不要放在 `docs/` 下，也不要在访客页面里插入维护说明链接
- 若修改 `docs/.vuepress/components/` 下的项目组件（如 `PostMasonry.vue`），必须同步更新 `editor-docs/` 中对应说明文档，保持组件与文档一致
- 涉及文章模板/卡片解析的改动，必须同时检查三处是否一致：`editor-docs/*.md`（维护约定）↔ `docs/.vuepress/components/*.vue`（渲染逻辑）↔ `scripts/new-*.mjs`（自动生成模板）
- 若三者之一发生变更（字段名、标签格式、默认值、路径约定等），应在同一次任务内同步更新另外两处，避免“文档说法、组件行为、脚本输出”不一致
